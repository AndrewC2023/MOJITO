{
  "name": "MOJITO Development Environment",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": true,
        "python.formatting.provider": "black"
      }
    }
  },

  "remoteUser": "root",
  "postCreateCommand": "git config --local core.autocrlf input && git config --local core.eol lf && git submodule update --init --recursive",

  "mounts": [
    "source=${localWorkspaceFolder}/dependencies,target=/workspaces/${localWorkspaceFolderBasename}/dependencies,type=bind,consistency=cached"
  ]
}
